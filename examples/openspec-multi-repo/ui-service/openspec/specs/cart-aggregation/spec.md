# cart aggregation Specification (local to ui-service)

## Purpose
How cart data from downstream services is aggregated for the UI.

> Local capability — internal to `ui-service`. Anything crossing a repo boundary
> belongs in the Store instead.

## Requirements

### Requirement: (replace with a real requirement)
#### Scenario: (replace with a real scenario)
- **WHEN** <trigger>
- **THEN** <observable outcome>
