# cart domain Specification (local to functional-service)

## Purpose
Domain rules for carts: creation, mutation, persistence, idempotency.

> Local capability — internal to `functional-service`. Anything crossing a repo boundary
> belongs in the Store instead.

## Requirements

### Requirement: (replace with a real requirement)
#### Scenario: (replace with a real scenario)
- **WHEN** <trigger>
- **THEN** <observable outcome>
